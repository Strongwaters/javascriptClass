//declare array
let arr =  [];
//create variable to store lowercase alphabet
let alph = 'abcdefghijklmnopqrstuvwxyz';
  //itterate through the length of alph
  for( let i = 0 ; i < alph.length; i++){
  //declare aBet to the iteration to show the charAt the positions
  aBet = alph.charAt(i);
  arr.push(aBet);
} 
///log after the for loop is complete
console.log(arr);

////complete!!!!
